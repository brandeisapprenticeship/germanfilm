<!DOCTYPE html>

<p id="notice"><%= notice %></p>
<h1>My Submissions </h1>

<table class="table">
    <thead>
    <tr>
      <th  style="color:white">Submission Title </th>
      <th  style="color:white"> Number Of Bookmarks</th>
    </tr>
     </thead>

  <tbody>
    <%if @submission != nil %>
    <% @submission.each do |submission| %>
      <tr>
        <td><%= link_to submission.title, submission, style:"color:#E8EE79"%></td>
        <td id="bookmarkcounter"><%= Bookmark.where(submission_id: submission.id).count%></td>
      </tr>
      <%end%>
  <%end%>
  </tbody>
</table>

<!-- <%= link_to 'New Submission', new_submission_path %> -->
