<p id="notice"><%= notice %></p>

<div class="btn-group" role="group" aria-label="...">
	<th>
		<%if @submission.author != current_user.name%>  <!-- needs to be for author only -->
		<button type="button" class="btn btn-default"><%= link_to 'Edit Submission', edit_submission_path(@submission) %> </button> 
		<%end%>
	</th>
  
  <!--need to fix path below -->
  <button type="button" class="btn btn-default"><%= link_to 'Return to Class Submissions', new_submission_path %> </button>
</div>

<h1> <%= @submission.title %></h1>
<h3> <%= @submission.notecard %>  </h3>
<h2> <%= @submission.vocabulary%></h2>

