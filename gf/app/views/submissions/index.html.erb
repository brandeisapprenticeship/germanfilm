<p id="notice"><%= notice %></p>

<h1>Class Submissions </h1>
<table class="table">
    <thead>
    <tr>
      <th colspan="9">Student Name </th><th colspan="3">Submission </th><th colspan="3"><th > Number Of Bookmarks</th><th colspan="3"><%if current_user.admin? %> Grade Out of 10 <%end%></th>
    </tr>
     </thead>

  <tbody>
    <% @submissions.each do |submission| %>
      <tr>
        <td> <%= current_user.name %></td>
        <td><%= submission.title %></td> <!-- NEED TO CHANGE LINK -->
        <td><%= 'number of bookmarks here' %></td>
        <%if current_user.admin?%>
          <td><%='grade here'%></td>
        <%end%>
       <br>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Submission', new_submission_path %>
